<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Profile</title>
    <style>
        .profile-container {
            background: #f4f8fb;
            min-height: 80vh;
            display: flex;
            justify-content: center;
            align-items: flex-start;
            padding: 40px 0;
        }
        .profile-card {
            background: #fff;
            border-radius: 12px;
            box-shadow: 0 4px 24px rgba(0, 0, 0, 0.08);
            padding: 32px 40px;
            max-width: 600px;
            width: 100%;
        }
        .profile-card h2 {
            margin-bottom: 16px;
            color: #003049;
            font-size: 2rem;
            font-weight: 700;
        }
        .profile-details {
            margin-bottom: 28px;
            padding-bottom: 16px;
            border-bottom: 1px solid #e0e6ed;
        }
        .profile-details h4 {
            margin-bottom: 8px;
            color: #007bff;
            font-size: 1.1rem;
        }
    </style>
</head>
<body>
    <%- include("partials/header") %>
    <div class="profile-container">
        <div class="profile-card">
            <h2>Welcome, <%= user ? user.name : 'User' %></h2>
            <div class="profile-details">
                <h4>Your Details</h4>
                <p><strong>Email:</strong> <%= user ? user.email : '' %></p>
                <p><strong>Phone:</strong> <%= user ? user.mobileNumber : '' %></p>
                <p><strong>Date of Birth:</strong> <%= user ? user.DOB : 'N/A' %></p>
                <p><strong>Gender:</strong> <%= user ? user.gender : 'N/A' %></p>
                <p><strong>Address:</strong> <%= user ? user.address : 'N/A' %></p>
            </div>
        </div>
    </div>
    <%- include("partials/footer") %>
</body>
</html>