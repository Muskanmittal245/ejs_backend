<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<style>
    /* Profile Page Styles */
.profile-container {
  background: #f4f8fb;
  min-height: 80vh;
  display: flex;
  justify-content: center;
  align-items: flex-start;
  padding: 40px 0;
}

.profile-card {
  background: #fff;
  border-radius: 12px;
  box-shadow: 0 4px 24px rgba(0,0,0,0.08);
  padding: 32px 40px;
  max-width: 600px;
  width: 100%;
}

.profile-card h2 {
  margin-bottom: 16px;
  color: #003049;
  font-size: 2rem;
  font-weight: 700;
}

.profile-details {
  margin-bottom: 28px;
  padding-bottom: 16px;
  border-bottom: 1px solid #e0e6ed;
}

.profile-details h4 {
  margin-bottom: 8px;
  color: #007bff;
  font-size: 1.1rem;
}

.profile-section {
  margin-bottom: 28px;
}

.profile-section h4 {
  color: #007bff;
  margin-bottom: 10px;
  font-size: 1.1rem;
}

.profile-list {
  list-style: none;
  padding: 0;
  margin: 0;
}

.profile-list li {
  padding: 10px 0;
  border-bottom: 1px solid #f0f0f0;
  font-size: 1rem;
  display: flex;
  align-items: center;
  gap: 8px;
}

.profile-list li:last-child {
  border-bottom: none;
}

.profile-date {
  color: #003049;
  font-weight: 600;
}

.profile-status.completed {
  color: #28a745;
  font-weight: 600;
}

.profile-status.upcoming {
  color: #ffc107;
  font-weight: 600;
}

.profile-record {
  color: #003049;
}

@media (max-width: 700px) {
  .profile-card {
    padding: 20px 10px;
    max-width: 98vw;
  }
}
</style>
<body>
    <%- include("partials/header") %>
<div class="profile-container">
  <div class="profile-card">
    <h2>Welcome, <%= user ? user.name : 'User' %></h2>
    <div class="profile-section">
      <h4>Past Appointments</h4>
      <% if (appointments && appointments.length) { %>
        <ul class="profile-list">
          <% appointments.forEach(function(app) { %>
            <li>
              <span class="profile-date"><strong><%= app.date %></strong></span>
              with Dr. <%= app.doctor %> - 
              <span class="profile-status <%= app.status === 'Completed' ? 'completed' : 'upcoming' %>">
                <%= app.status %>
              </span>
            </li>
          <% }) %>
        </ul>
      <% } else { %>
        <p>No appointments found.</p>
      <% } %>
    </div>
    
  </div>
</div>
<%- include("partials/footer") %>
</body>
</html>